var svg = null;

function drawHeadToHead(dataArr) {
    /**
    var width = 500,
        height = 75

    //console.log(d3.sum(data));

    chart = d3.select("#chartz")
        .attr("width", width)
        .attr("height", height)
        .style("display", 'block')
        .style('margin', 'auto')
        .style('preserveAspectRatio', 'xMidYMid')

    chart.selectAll('rect').data(dataArr)
        .attr('id', 'occupancyRect')
        .enter()
        .append("rect")
        .attr("x", function (d) {
            return current += spacer
        })
        .attr("y", 25)
        .attr("width", 60)
        .attr("height", 50)
        .attr('fill', function (d) {
            return d == 'R' ? "#c5111e" : "#1110a6"
        })
        .style("stroke", "#f7fcff")
        .style("stroke-width", 2)

    spacer = 60
    current = 15
    chart.selectAll('text').data(dataArrKeys)
        .attr('id', 'occupancyText')
        .enter()
        .append("text")
        .attr("x", function (d) {
            return current += spacer
        })
        .attr("y", 52)
        .attr('fill', function (d) {
            return "#f7fcff"
        })
        .text(function (d) {
            return d;
        })
     **/
}
